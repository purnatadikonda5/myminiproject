import React, { useState } from "react";

const UserSelectionModal = ({ isOpen, onClose, users }) => {

    if (!isOpen) return null;

    return (
        
    );
};

export default UserSelectionModal;
